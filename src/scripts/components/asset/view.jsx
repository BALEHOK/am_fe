/**
 * @jsx React.DOM
 */

var React = require('react');

var AssetView = React.createClass({
    render: function() {
        return (
        	<div>
        		<h1>Asset View Page</h1>
        	</div>
       	);
    }
});
module.exports = AssetView;